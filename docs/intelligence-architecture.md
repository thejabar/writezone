should define:

* What a Signal is.
* What a Processor is.
* What a FeedCandidate is.
* The rule that one processor produces one signal.
* The rule that processors never rank.
* The rule that ranking consumes signals.